# nfpm changelog — rendered into the Debian changelog and the rpm %changelog.
# Keep newest entry first. Dates are RFC-3339.
- semver: 0.9.8.7
  date: 2026-08-06T00:00:00Z
  packager: NetLOX Inc. <support@netlox.io>
  changes:
    - note: Initial packaged release of the LoxiLB Inference Gateway.
    - note: Ships the loxilb load balancer, the loxilb-mcp bridge, the eBPF datapath objects, and a systemd service.
    - note: Version tags now follow loxilb-io/loxilb (vMAJOR.MINOR.PATCH[.BUILD]); the reported version is derived from the release tag rather than pinned in source.
