# The --fr 4 self-check extracts the per-model vectors doc here at runtime (generated; do not commit)
.fr4-vectors-*.json
# scenario-5 long-context runtime artifacts (generated by validation.sh; do not commit)
.corpus-longctx.json
.longctx-*
