apiVersion: 1
datasources:
  - name: Prometheus
    uid: loxilb-prom
    type: prometheus
    access: proxy
    url: http://localhost:9090
    isDefault: true
    jsonData:
      timeInterval: 10s   # match scrape_interval so $__rate_interval resolves sanely
